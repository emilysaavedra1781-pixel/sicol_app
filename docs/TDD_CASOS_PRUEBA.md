# Documentación de Tests — 3 Momentos del TDD (SICOL)

Este documento detalla los casos de prueba implementados en el proyecto SICOL, estructurados bajo la metodología de los 3 momentos del TDD: Análisis, Ficha Técnica y Código Real.

---

## RF01 — Registro de Usuario (Pasajero)

| # | Escenario | MOMENTO 1 — ¿Qué pasa si...? | MOMENTO 2 — Ficha de caso de prueba (Precondición / Acción / Resultado esperado) | MOMENTO 3 — Test en código (Dart / flutter_test) |
|---|---|---|---|---|
| CP01 | Registro exitoso | ¿Qué pasa si el pasajero ingresa todos sus datos válidos y confirma el OTP? | **Precondición**: FakeDb y MockAuth inicializados. <br> **Acción**: `service.registerPasajero(...)` con datos completos. <br> **Resultado esperado**: `success: true`, usuario creado en Auth con email sintético `@sicol.pe` y documento en la colección `usuarios`. <br> **Resultado obtenido**: ✅ success: true | ```dart\ntest('RF01 CP01 - Registro exitoso: registerPasajero crea el usuario en Auth y Firestore', () async {\n  final fakeDb = FakeFirebaseFirestore();\n  final mockAuth = MockFirebaseAuth();\n  final service = AuthService(auth: mockAuth, db: fakeDb);\n  final result = await service.registerPasajero(\n    dni: '12345678', nombre: 'Juan', apellido: 'Perez', celular: '987654321', email: 'juan@test.com', password: 'password123', fechaNacimiento: '01/01/1990',\n  );\n  expect(result['success'], isTrue);\n  expect(mockAuth.currentUser!.email, equals('987654321@sicol.pe'));\n  final snap = await fakeDb.collection('usuarios').doc(mockAuth.currentUser!.uid).get();\n  expect(snap.exists, isTrue);\n});``` |
| CP02 | Validación de datos | ¿Qué pasa si se ingresan datos con formato incorrecto (DNI corto, celular inválido)? | **Precondición**: `ValidationService` disponible. <br> **Acción**: Llamar a métodos de validación con inputs inválidos. <br> **Resultado esperado**: Retorno de mensajes de error específicos por campo. <br> **Resultado obtenido**: ✅ Errores detectados | ```dart\ntest('RF01 CP02 - Validación de datos: ValidationService detecta campos inválidos', () {\n  expect(ValidationService.validarDni('123'), equals('El DNI debe tener 8 dígitos'));\n  expect(ValidationService.validarCelular('123'), equals('9 dígitos (Ej. 9XXXXXXXX)'));\n  expect(ValidationService.validarEmail('test'), equals('Email inválido'));\n  expect(ValidationService.validarPassword('123'), equals('Mínimo 8 caracteres'));\n});``` |
| CP03 | Celular ya registrado | ¿Qué pasa si el pasajero intenta registrarse con un número que ya existe en el sistema? | **Precondición**: MockAuth configurado para lanzar `email-already-in-use`. <br> **Acción**: `service.registerPasajero(...)`. <br> **Resultado esperado**: `success: false` con error `duplicate-phone`. <br> **Resultado obtenido**: ✅ error: duplicate-phone | ```dart\ntest('RF01 CP03 - Celular duplicado: registerPasajero retorna error duplicate-phone', () async {\n  final fakeDb = FakeFirebaseFirestore();\n  final mockAuth = MockFirebaseAuth();\n  whenCalling(Invocation.method(#createUserWithEmailAndPassword, null)).on(mockAuth).thenThrow(FirebaseAuthException(code: 'email-already-in-use'));\n  final service = AuthService(auth: mockAuth, db: fakeDb);\n  final result = await service.registerPasajero(dni: '12345678', nombre: 'Juan', apellido: 'Perez', celular: '987654321', email: 'juan@test.com', password: 'password123', fechaNacimiento: '01/01/1990');\n  expect(result['success'], isFalse);\n  expect(result['error'], equals('duplicate-phone'));\n});``` |
| CP04 | OTP incorrecto | ¿Qué pasa si el usuario ingresa un código de verificación que no coincide con el enviado? | **Precondición**: MockAuth configurado para lanzar `invalid-verification-code`. <br> **Acción**: `service.verifyOTP(...)`. <br> **Resultado esperado**: Lanzamiento de `FirebaseAuthException`. <br> **Resultado obtenido**: ✅ Exception lanzada | ```dart\ntest('RF01 CP04 - OTP incorrecto: verifyOTP lanza FirebaseAuthException', () async {\n  final mockAuth = MockFirebaseAuth();\n  final fakeDb = FakeFirebaseFirestore();\n  whenCalling(Invocation.method(#signInWithCredential, null)).on(mockAuth).thenThrow(FirebaseAuthException(code: 'invalid-verification-code'));\n  final service = AuthService(auth: mockAuth, db: fakeDb);\n  expect(() => service.verifyOTP(verificationId: 'vid', smsCode: '000000'), throwsA(isA<FirebaseAuthException>().having((e) => e.code, 'code', 'invalid-verification-code')));\n});``` |
| CP05 | Error de red | ¿Qué pasa si la conexión a internet falla durante el proceso de registro? | **Precondición**: MockAuth configurado para lanzar `network-request-failed`. <br> **Acción**: `service.registerPasajero(...)`. <br> **Resultado esperado**: `success: false` con error `network-error`. <br> **Resultado obtenido**: ✅ error: network-error | ```dart\ntest('RF01 CP05 - Error de red: registerPasajero retorna error network-error', () async {\n  final mockAuth = MockFirebaseAuth();\n  final fakeDb = FakeFirebaseFirestore();\n  whenCalling(Invocation.method(#createUserWithEmailAndPassword, null)).on(mockAuth).thenThrow(FirebaseAuthException(code: 'network-request-failed'));\n  final service = AuthService(auth: mockAuth, db: fakeDb);\n  final result = await service.registerPasajero(dni: '12345678', nombre: 'Juan', apellido: 'Perez', celular: '987654321', email: 'juan@test.com', password: 'password123', fechaNacimiento: '01/01/1990');\n  expect(result['success'], isFalse);\n  expect(result['error'], equals('network-error'));\n});``` |

---

## RF02 — Inicio de Sesión del Pasajero / RF40 — Bloqueo Automático

| # | Escenario | MOMENTO 1 — ¿Qué pasa si...? | MOMENTO 2 — Ficha de caso de prueba (Precondición / Acción / Resultado esperado) | MOMENTO 3 — Test en código (Dart / flutter_test) |
|---|---|---|---|---|
| CP01 | Login exitoso | ¿Qué pasa si el pasajero ingresa sus credenciales correctas? | **Precondición**: Usuario creado en FakeDb con estado activo. <br> **Acción**: `service.loginWithCelular(...)`. <br> **Resultado esperado**: `success: true`, `rol: pasajero`. <br> **Resultado obtenido**: ✅ success: true | ```dart\ntest('RF02 CP01 - Flujo exitoso: loginWithCelular retorna success=true y rol=pasajero con credenciales válidas', () async {\n  final fakeDb = FakeFirebaseFirestore();\n  await _crearUsuarioPasajero(fakeDb, celular: '987654321');\n  final mockAuth = MockFirebaseAuth();\n  final service = AuthService(auth: mockAuth, db: fakeDb);\n  final result = await service.loginWithCelular(celular: '987654321', password: 'contraseña123');\n  expect(result['success'], isTrue);\n  expect(result['rol'], equals('pasajero'));\n});``` |
| CP02 | Celular no registrado | ¿Qué pasa si se intenta loguear un número que no existe en Firestore? | **Precondición**: FakeDb vacío. <br> **Acción**: `service.loginWithCelular(...)`. <br> **Resultado esperado**: `success: false`, error `usuario_no_encontrado`. <br> **Resultado obtenido**: ✅ error: usuario_no_encontrado | ```dart\ntest('RF02 CP02 - Celular no registrado: loginWithCelular retorna error=usuario_no_encontrado', () async {\n  final fakeDb = FakeFirebaseFirestore();\n  final mockAuth = MockFirebaseAuth();\n  final service = AuthService(auth: mockAuth, db: fakeDb);\n  final result = await service.loginWithCelular(celular: '999999999', password: 'cualquierClave');\n  expect(result['success'], isFalse);\n  expect(result['error'], equals('usuario_no_encontrado'));\n});``` |
| CP05 | Bloqueo automático | ¿Qué pasa si el pasajero falla la contraseña 3 veces seguidas? | **Precondición**: Usuario activo. MockAuth configurado para fallar. <br> **Acción**: Intentar login 3 veces. <br> **Resultado esperado**: Al 3er intento, `bloqueado: true` en Firestore y error `cuenta_bloqueada`. <br> **Resultado obtenido**: ✅ cuenta_bloqueada | ```dart\ntest('RF02 CP05 - Bloqueo automático: después de 3 intentos fallidos el sistema bloquea la cuenta automáticamente', () async {\n  final fakeDb = FakeFirebaseFirestore();\n  await _crearUsuarioPasajero(fakeDb, celular: '922222222', intentosFallidos: 0);\n  final mockAuth = MockFirebaseAuth();\n  _hacerQueAuthFalle(mockAuth);\n  final service = AuthService(auth: mockAuth, db: fakeDb);\n  await service.loginWithCelular(celular: '922222222', password: 'mal1');\n  await service.loginWithCelular(celular: '922222222', password: 'mal2');\n  final intento3 = await service.loginWithCelular(celular: '922222222', password: 'mal3');\n  expect(intento3['error'], equals('cuenta_bloqueada'));\n  final snap = await fakeDb.collection('usuarios').where('celular', isEqualTo: '922222222').limit(1).get();\n  expect(snap.docs.first.data()['bloqueado'], isTrue);\n});``` |
| CP03 | Desbloqueo exitoso | ¿Qué pasa si se solicita el desbloqueo tras recuperar la contraseña? | **Precondición**: Usuario con `bloqueado: true`. <br> **Acción**: `service.desbloquearCuentaPorCelular(...)`. <br> **Resultado esperado**: `bloqueado: false`, `estado: activo` en Firestore. <br> **Resultado obtenido**: ✅ desbloqueado | ```dart\ntest('RF40 CP03 - Desbloqueo exitoso: desbloquearCuentaPorCelular restablece bloqueado=false y estado=activo en Firestore', () async {\n  final fakeDb = FakeFirebaseFirestore();\n  await _crearUsuarioPasajero(fakeDb, celular: '955555555', bloqueado: true, estado: 'bloqueado');\n  final mockAuth = MockFirebaseAuth();\n  final service = AuthService(auth: mockAuth, db: fakeDb);\n  await service.desbloquearCuentaPorCelular('955555555');\n  final snap = await fakeDb.collection('usuarios').where('celular', isEqualTo: '955555555').limit(1).get();\n  expect(snap.docs.first.data()['bloqueado'], isFalse);\n  expect(snap.docs.first.data()['estado'], equals('activo'));\n});``` |

---

## RF03 — Recuperación de Contraseña del Pasajero

| # | Escenario | MOMENTO 1 — ¿Qué pasa si...? | MOMENTO 2 — Ficha de caso de prueba (Precondición / Acción / Resultado esperado) | MOMENTO 3 — Test en código (Dart / flutter_test) |
|---|---|---|---|---|
| CP01 | Flujo exitoso | ¿Qué pasa si el pasajero completa todos los pasos de la recuperación OTP? | **Precondición**: FakeFunctions configurado para éxito. <br> **Acción**: Llamar a solicitar, verificar y cambiarPassword. <br> **Resultado esperado**: Todas las fases retornan `success: true`. <br> **Resultado obtenido**: ✅ success: true | ```dart\ntest('RF03 CP01 - Flujo exitoso: solicita, verifica y cambia contraseña', () async {\n  final fakeDb = FakeFirebaseFirestore();\n  final mockAuth = MockFirebaseAuth();\n  final fakeCallable = FakeHttpsCallable((params) async {\n    if (params.containsKey('email') && !params.containsKey('otp')) return {'success': true};\n    if (params.containsKey('otp')) return {'success': true, 'uid': 'user-123'};\n    if (params.containsKey('newPassword')) return {'success': true};\n    return {};\n  });\n  final service = AuthService(auth: mockAuth, db: fakeDb, functions: FakeFirebaseFunctions(fakeCallable));\n  final res1 = await service.solicitarOtpRecuperacion('pasajero@test.com');\n  expect(res1['success'], isTrue);\n  final res2 = await service.verificarOtpRecuperacion('pasajero@test.com', '123456');\n  expect(res2['success'], isTrue);\n  final res3 = await service.cambiarPasswordSeguro('user-123', 'nueva_clave_2026');\n  expect(res3['success'], isTrue);\n});``` |
| CP02 | Usuario no registrado | ¿Qué pasa si se pide OTP para un email inexistente? | **Precondición**: Backend devuelve `not-found`. <br> **Acción**: `solicitarOtpRecuperacion(...)`. <br> **Resultado esperado**: `error: usuario_no_encontrado`. <br> **Resultado obtenido**: ✅ usuario_no_encontrado | ```dart\ntest('RF03 CP02 - Usuario no registrado: retorna error usuario_no_encontrado', () async {\n  final fakeDb = FakeFirebaseFirestore();\n  final mockAuth = MockFirebaseAuth();\n  final fakeCallable = FakeHttpsCallable((params) async {\n    throw FirebaseFunctionsException(code: 'not-found', message: 'User not found');\n  });\n  final service = AuthService(auth: mockAuth, db: fakeDb, functions: FakeFirebaseFunctions(fakeCallable));\n  final result = await service.solicitarOtpRecuperacion('inexistente@test.com');\n  expect(result['success'], isFalse);\n  expect(result['error'], equals('usuario_no_encontrado'));\n});``` |

---

## RF06 — Seleccionar Colectivo Disponible

| # | Escenario | MOMENTO 1 — ¿Qué pasa si...? | MOMENTO 2 — Ficha de caso de prueba (Precondición / Acción / Resultado esperado) | MOMENTO 3 — Test en código (Dart / flutter_test) |
|---|---|---|---|---|
| CP01 | Listar colectivos | ¿Qué pasa si el pasajero busca colectivos en una ruta activa? | **Precondición**: Un viaje activo y uno finalizado en FakeDb. <br> **Acción**: `service.getColectivosDisponibles(ruta: 'chosica_lima')`. <br> **Resultado esperado**: El stream emite solo 1 viaje (el activo). <br> **Resultado obtenido**: ✅ 1 viaje devuelto | ```dart\ntest('RF06 CP01 - Listar colectivos: getColectivosDisponibles retorna viajes con estado activo', () async {\n  final fakeDb = FakeFirebaseFirestore();\n  final mockAuth = MockFirebaseAuth(signedIn: true);\n  await fakeDb.collection('viajes').add({ 'conductorNombre': 'Pedro Picapiedra', 'estado': 'activo', 'ruta': 'chosica_lima' });\n  await fakeDb.collection('viajes').add({ 'conductorNombre': 'Pablo Marmol', 'estado': 'finalizado', 'ruta': 'chosica_lima' });\n  final service = BookingService(auth: mockAuth, db: fakeDb);\n  final stream = service.getColectivosDisponibles(ruta: 'chosica_lima');\n  final snap = await stream.first;\n  expect(snap.docs.length, equals(1));\n});``` |
| CP03 | Concurrencia | ¿Qué pasa si el colectivo se llena mientras el pasajero lo seleccionaba? | **Precondición**: Viaje con 3 ocupados y 1 bloqueado (total = capacidad 4). <br> **Acción**: `service.verificarDisponibilidadViaje(...)`. <br> **Resultado esperado**: `false` (no disponible). <br> **Resultado obtenido**: ✅ false | ```dart\ntest('RF06 CP03 - Concurrencia: verificarDisponibilidadViaje retorna false si el viaje está lleno (ocupados + bloqueados)', () async {\n  final fakeDb = FakeFirebaseFirestore();\n  final mockAuth = MockFirebaseAuth(signedIn: true);\n  final viajeRef = await fakeDb.collection('viajes').add({\n    'capacidad': 4, 'asientosOcupados': 3, 'asientos': { 'asiento_4': {'estado': 'bloqueado'} }\n  });\n  final service = BookingService(auth: mockAuth, db: fakeDb);\n  final disponible = await service.verificarDisponibilidadViaje(viajeRef.id);\n  expect(disponible, isFalse);\n});``` |

---

## RF07 — Compra de Pasaje y Selección de Asiento / RF50 — Registro de Viajero

| # | Escenario | MOMENTO 1 — ¿Qué pasa si...? | MOMENTO 2 — Ficha de caso de prueba (Precondición / Acción / Resultado esperado) | MOMENTO 3 — Test en código (Dart / flutter_test) |
|---|---|---|---|---|
| CP01 | Reserva exitosa | ¿Qué pasa si se envían datos válidos de asiento y viajero al backend? | **Precondición**: Usuario logueado. <br> **Acción**: `service.bloquearAsientosYCrearPreferencia(...)`. <br> **Resultado esperado**: Se recibe `initPoint` de Mercado Pago y `reservaGroupId`. <br> **Resultado obtenido**: ✅ Datos recibidos | ```dart\ntest('RF07 CP01 / RF50 CP01 - Reserva exitosa: llama a la función de backend con datos correctos', () async {\n  final mockAuth = MockFirebaseAuth(signedIn: true);\n  final fakeDb = FakeFirebaseFirestore();\n  final fakeCallable = FakeHttpsCallable((params) async {\n    return { 'init_point': 'https://mercadopago.com/checkout/123', 'reservaGroupId': 'group-abc' };\n  });\n  final service = BookingService(auth: mockAuth, db: fakeDb, functions: FakeFirebaseFunctions(fakeCallable));\n  final result = await service.bloquearAsientosYCrearPreferencia(viajeId: 'v123', asientos: [2], viajeros: [{'asiento': 2, 'nombre': 'Juan'}], monto: 15.0, paradero: 'Ate');\n  expect(result['success'], isTrue);\n  expect(result['initPoint'], contains('mercadopago.com'));\n});``` |
| CP02 | Validación viajero | ¿Qué pasa si se intenta registrar un viajero con DNI incompleto? | **Precondición**: `ValidationService` disponible. <br> **Acción**: `ValidationService.esViajeroValido(...)`. <br> **Resultado esperado**: `false`. <br> **Resultado obtenido**: ✅ false | ```dart\ntest('RF50 CP02 - Validación: esViajeroValido detecta datos incompletos', () {\n  expect(ValidationService.esViajeroValido('', '12345678'), isFalse);\n  expect(ValidationService.esViajeroValido('Juan', '123'), isFalse);\n});``` |

---

## RF08 — Confirmación de Compra de Pasaje

| # | Escenario | MOMENTO 1 — ¿Qué pasa si...? | MOMENTO 2 — Ficha de caso de prueba (Precondición / Acción / Resultado esperado) | MOMENTO 3 — Test en código (Dart / flutter_test) |
|---|---|---|---|---|
| CP03 | Cancelación manual | ¿Qué pasa si el pasajero retrocede en el resumen de compra? | **Precondición**: Asientos en estado `bloqueado` en FakeDb. <br> **Acción**: `service.cancelarBloqueoTemporal(...)`. <br> **Resultado esperado**: Asientos vuelven a `libre` y reservas se eliminan. <br> **Resultado obtenido**: ✅ Asientos liberados | ```dart\ntest('RF08 CP03 - Cancelación manual: libera asientos en viaje y borra documentos de reserva', () async {\n  final fakeDb = FakeFirebaseFirestore();\n  final mockAuth = MockFirebaseAuth(signedIn: true);\n  final viajeRef = await fakeDb.collection('viajes').add({ 'asientos': { 'asiento_5': {'estado': 'bloqueado'} } });\n  await fakeDb.collection('reservas').add({ 'reservaGroupId': 'g999', 'viajeId': viajeRef.id, 'numeroAsiento': 5 });\n  final service = BookingService(db: fakeDb, auth: mockAuth);\n  await service.cancelarBloqueoTemporal(viajeId: viajeRef.id, reservaGroupId: 'g999', asientos: [5]);\n  final vSnap = await viajeRef.get();\n  expect(vSnap.data()!['asientos']['asiento_5']['estado'], equals('libre'));\n});``` |

---

## RF09 — Pago de Pasaje con División Automática

| # | Escenario | MOMENTO 1 — ¿Qué pasa si...? | MOMENTO 2 — Ficha de caso de prueba (Precondición / Acción / Resultado esperado) | MOMENTO 3 — Test en código (Dart / flutter_test) |
|---|---|---|---|---|
| CP01 | Comprobante | ¿Qué pasa si el backend genera el objeto comprobante en Firestore? | **Precondición**: Mapa de datos con sub-objeto `comprobante`. <br> **Acción**: `ComprobanteModel.fromFirestore(...)`. <br> **Resultado esperado**: Mapeo correcto de `codigoComprobante`, `montoFormateado` y fecha. <br> **Resultado obtenido**: ✅ Modelo poblado | ```dart\ntest('RF09 CP01 - Comprobante: ComprobanteModel mapea correctamente los datos del backend', () {\n  final data = { 'nombreViajero': 'Juan', 'paymentId': 'PAY-1', 'comprobante': { 'codigoComprobante': 'CMP-1', 'monto': 15.0 } };\n  final model = ComprobanteModel.fromFirestore(data, 'fallback');\n  expect(model.codigoComprobante, equals('CMP-1'));\n  expect(model.montoFormateado, equals('S/ 15.00'));\n});``` |
| CP04 | Concurrencia | ¿Qué pasa si el asiento es tomado por otro justo antes de confirmar el pago? | **Precondición**: Reserva en estado `bloqueada`. <br> **Acción**: `service.getReservaStream(...)` y actualizar a `error_concurrencia`. <br> **Resultado esperado**: El stream emite el nuevo estado. <br> **Resultado obtenido**: ✅ Cambio detectado | ```dart\ntest('RF09 CP04 - Concurrencia: getReservaStream permite detectar cambios de estado en tiempo real', () async {\n  final fakeDb = FakeFirebaseFirestore();\n  final mockAuth = MockFirebaseAuth(signedIn: true);\n  final resRef = await fakeDb.collection('reservas').add({ 'estado': 'bloqueada' });\n  final service = BookingService(db: fakeDb, auth: mockAuth, functions: FakeFirebaseFunctions());\n  final stream = service.getReservaStream(resRef.id);\n  final expectation = expectLater(stream.map((snap) => snap.data()!['estado']), emitsInOrder(['bloqueada', 'error_concurrencia']));\n  await resRef.update({'estado': 'error_concurrencia'});\n  await expectation;\n});``` |

---

## RF11 — Cancelación de Reserva / RF28 — Reasignación de Asiento

| # | Escenario | MOMENTO 1 — ¿Qué pasa si...? | MOMENTO 2 — Ficha de caso de prueba (Precondición / Acción / Resultado esperado) | MOMENTO 3 — Test en código (Dart / flutter_test) |
|---|---|---|---|---|
| CP01 | Cancelación exitosa | ¿Qué pasa si el pasajero cancela antes de que el viaje arranque? | **Precondición**: Viaje con ingreso acumulado y reserva confirmada. <br> **Acción**: `service.cancelarReserva(...)`. <br> **Resultado esperado**: Ingreso del conductor disminuye, asiento queda libre. <br> **Resultado obtenido**: ✅ Saldo y asiento corregidos | ```dart\ntest('RF11 CP01 - Cancelación exitosa: libera asiento y descuenta ingreso', () async {\n  final fakeDb = FakeFirebaseFirestore();\n  final mockAuth = MockFirebaseAuth(signedIn: true);\n  final viajeRef = await fakeDb.collection('viajes').add({ 'ingresoTotal': 15.0, 'asientos': {'asiento_3': {'estado': 'ocupado'}} });\n  final resRef = await fakeDb.collection('reservas').add({ 'viajeId': viajeRef.id, 'numeroAsiento': 3, 'estado': 'confirmada', 'monto': 15.0 });\n  final service = BookingService(db: fakeDb, auth: mockAuth, functions: FakeFirebaseFunctions());\n  await service.cancelarReserva(reservaId: resRef.id, viajeId: viajeRef.id, numeroAsiento: 3);\n  final vSnap = await viajeRef.get();\n  expect(vSnap.data()!['ingresoTotal'], equals(0.0));\n  expect(vSnap.data()!['asientos']['asiento_3']['estado'], equals('libre'));\n});``` |
| CP01 | Cambio de asiento | ¿Qué pasa si el pasajero desea cambiarse a un asiento que quedó libre? | **Precondición**: Reserva en asiento 1, asiento 2 libre. <br> **Acción**: `service.cambiarAsiento(nuevo: 2)`. <br> **Resultado esperado**: Reserva actualizada a 2, asiento 1 queda libre en viaje. <br> **Resultado obtenido**: ✅ Reasignación exitosa | ```dart\ntest('RF28 CP01 - Cambio de asiento: actualiza viaje y reserva', () async {\n  final fakeDb = FakeFirebaseFirestore();\n  final mockAuth = MockFirebaseAuth(signedIn: true);\n  final viajeRef = await fakeDb.collection('viajes').add({ 'asientos': { 'asiento_1': {'estado': 'ocupado'}, 'asiento_2': {'estado': 'libre'} } });\n  final resRef = await fakeDb.collection('reservas').add({ 'viajeId': viajeRef.id, 'numeroAsiento': 1, 'estado': 'confirmada' });\n  final service = BookingService(db: fakeDb, auth: mockAuth, functions: FakeFirebaseFunctions());\n  await service.cambiarAsiento(reservaId: resRef.id, viajeId: viajeRef.id, nuevoAsiento: 2);\n  final rSnap = await resRef.get();\n  expect(rSnap.data()!['numeroAsiento'], equals(2));\n});``` |

---

## RF53 — Registro de Conductor / RF16 — Perfil / RF26 — Administración

| # | Escenario | MOMENTO 1 — ¿Qué pasa si...? | MOMENTO 2 — Ficha de caso de prueba (Precondición / Acción / Resultado esperado) | MOMENTO 3 — Test en código (Dart / flutter_test) |
|---|---|---|---|---|
| CP03 | Placa duplicada | ¿Qué pasa si se intenta registrar un vehículo con placa ya existente? | **Precondición**: Placa `ABC-123` en FakeDb. <br> **Acción**: `service.isPlacaRegistered('ABC-123')`. <br> **Resultado esperado**: `true`. <br> **Resultado obtenido**: ✅ true | ```dart\ntest('RF53 CP03 - Placa duplicada: isPlacaRegistered detecta colisiones', () async {\n  final fakeDb = FakeFirebaseFirestore();\n  await fakeDb.collection('usuarios').add({ 'vehiculo': {'placa': 'ABC-123'} });\n  final service = AuthService(db: fakeDb);\n  expect(await service.isPlacaRegistered('ABC-123'), isTrue);\n});``` |
| CP06 | Aprobación cuenta | ¿Qué pasa si el Admin aprueba a un conductor pendiente? | **Precondición**: Conductor en estado `pendiente_aprobacion`. <br> **Acción**: `service.aprobarConductor(...)`. <br> **Resultado esperado**: Estado cambia a `activo` y se genera código `COND-XXXX`. <br> **Resultado obtenido**: ✅ Código generado | ```dart\ntest('RF26 CP06 - Aprobación de cuenta: aprobarConductor cambia estado a activo y genera código', () async {\n  final fakeDb = FakeFirebaseFirestore();\n  final uid = 'c123';\n  await fakeDb.collection('usuarios').doc(uid).set({ 'estado': 'pendiente_aprobacion' });\n  final service = AuthService(db: fakeDb);\n  final codigo = await service.aprobarConductor(uid);\n  final snap = await fakeDb.collection('usuarios').doc(uid).get();\n  expect(snap.data()!['estado'], equals('activo'));\n  expect(codigo, startsWith('COND-'));\n});``` |
| CP09 | Exclusión UID | ¿Qué pasa si el Admin edita un conductor y guarda su propia placa? | **Precondición**: Conductor B con su placa. <br> **Acción**: `isDniRegistered(dniB, excludeUid: B)`. <br> **Resultado esperado**: `false` (no es duplicado de otro). <br> **Resultado obtenido**: ✅ false | ```dart\ntest('RF26 CP09 / CP10 - Duplicados en edición: isDniRegistered/isCelularRegistered soporta exclusión de UID', () async {\n  final fakeDb = FakeFirebaseFirestore();\n  await fakeDb.collection('usuarios').doc('uid-B').set({'dni': '2222'});\n  final service = AuthService(db: fakeDb);\n  expect(await service.isDniRegistered('2222', excludeUid: 'uid-B'), isFalse);\n});``` |

---

## RF14 — Notificación de Proximidad / RF19 — ETA / RF27 — Retraso

| # | Escenario | MOMENTO 1 — ¿Qué pasa si...? | MOMENTO 2 — Ficha de caso de prueba (Precondición / Acción / Resultado esperado) | MOMENTO 3 — Test en código (Dart / flutter_test) |
|---|---|---|---|---|
| CP01 | Estimación ETA | ¿Qué pasa si el colectivo está a 1km de distancia del paradero? | **Precondición**: Lógica de cálculo 500m/min. <br> **Acción**: `calculateETA(1000)`. <br> **Resultado esperado**: "Llega en aprox. 2 min". <br> **Resultado obtenido**: ✅ 2 min | ```dart\ntest('RF19 CP01 - Estimación de tiempo: valida la lógica matemática de MapaSeguimientoInline', () {\n  String calculateETA(double distanceMeters) {\n    if (distanceMeters < 100) return \"Llegando...\";\n    return \"Llega en aprox. ${(distanceMeters / 500).ceil()} min\";\n  }\n  expect(calculateETA(1000), equals(\"Llega en aprox. 2 min\"));\n});``` |
| CP01 | Proximidad | ¿Qué pasa si el vehículo entra al radio de 300m del pasajero? | **Precondición**: Lógica de backend definida (300m threshold). <br> **Acción**: Comparar distancia contra umbral. <br> **Resultado esperado**: `true` si es <= 300. <br> **Resultado obtenido**: ✅ Lógica validada | ```dart\ntest('RF14 CP01 - Proximidad: Documentación de lógica de backend', () {\n  const proximityThreshold = 300;\n  double distance1 = 250;\n  expect(distance1 <= proximityThreshold, isTrue);\n});``` |

---

## RF34 — Inicio de Viaje / RF33 — Conductor Activo / RF42 — Arranque

| # | Escenario | MOMENTO 1 — ¿Qué pasa si...? | MOMENTO 2 — Ficha de caso de prueba (Precondición / Acción / Resultado esperado) | MOMENTO 3 — Test en código (Dart / flutter_test) |
|---|---|---|---|---|
| CP01 | Inicio exitoso | ¿Qué pasa si el conductor inicia viaje dentro del rango de 500m? | **Precondición**: MockLocation con `withinRange: true`. <br> **Acción**: `service.iniciarViaje(...)`. <br> **Resultado esperado**: `success: true`, viaje creado en Firestore. <br> **Resultado obtenido**: ✅ success: true | ```dart\ntest('RF34 CP01 - Inicio exitoso: crea viaje si está dentro del rango', () async {\n  final fakeDb = FakeFirebaseFirestore();\n  final mockLocation = MockLocationService();\n  final service = TripService(auth: MockFirebaseAuth(signedIn: true), db: fakeDb, locationService: mockLocation);\n  final result = await service.iniciarViaje(conductorData: {'vehiculo': {'capacidad': 4}});\n  expect(result['success'], isTrue);\n  final doc = await fakeDb.collection('viajes').doc(result['viajeId']).get();\n  expect(doc.data()!['estado'], equals('activo'));\n});``` |
| CP01 | Bloqueo simultáneo | ¿Qué pasa si el conductor intenta iniciar un segundo viaje sin cerrar el actual? | **Precondición**: Viaje activo existente en FakeDb. <br> **Acción**: `service.iniciarViaje(...)`. <br> **Resultado esperado**: `error: active-trip-exists`. <br> **Resultado obtenido**: ✅ active-trip-exists | ```dart\ntest('RF33 CP01 - Bloqueo simultáneo: no permite iniciar viaje si ya existe uno activo', () async {\n  final fakeDb = FakeFirebaseFirestore();\n  final mockAuth = MockFirebaseAuth(signedIn: true);\n  await fakeDb.collection('viajes').add({ 'conductorUid': mockAuth.currentUser!.uid, 'estado': 'activo' });\n  final service = TripService(auth: mockAuth, db: fakeDb);\n  final result = await service.iniciarViaje(conductorData: {});\n  expect(result['error'], equals('active-trip-exists'));\n});``` |

---

## RF23 — Login Conductor / RF46 — Recuperación Conductor

| # | Escenario | MOMENTO 1 — ¿Qué pasa si...? | MOMENTO 2 — Ficha de caso de prueba (Precondición / Acción / Resultado esperado) | MOMENTO 3 — Test en código (Dart / flutter_test) |
|---|---|---|---|---|
| CP01 | Login exitoso | ¿Qué pasa si el conductor usa su código `COND-0001` y contraseña? | **Precondición**: Conductor registrado en FakeDb. <br> **Acción**: `service.loginConductor(...)`. <br> **Resultado esperado**: `success: true`, `rol: conductor`. <br> **Resultado obtenido**: ✅ success: true | ```dart\ntest('RF23 CP01 - Login exitoso: retorna success=true y rol=conductor', () async {\n  final fakeDb = FakeFirebaseFirestore();\n  await _crearConductor(fakeDb, codigo: 'COND-0001', celular: '988888888');\n  final service = AuthService(auth: MockFirebaseAuth(), db: fakeDb, functions: FakeFirebaseFunctions(FakeHttpsCallable((p) async => {})));\n  final result = await service.loginConductor(codigoConductor: 'COND-0001', password: 'password123');\n  expect(result['success'], isTrue);\n  expect(result['rol'], equals('conductor'));\n});``` |
| CP02 | OTP incorrecto | ¿Qué pasa si se ingresa un código de recuperación inválido? | **Precondición**: Backend devuelve `invalid-argument`. <br> **Acción**: `verificarOtpRecuperacion(...)`. <br> **Resultado esperado**: `error: invalid-otp`. <br> <br> **Resultado obtenido**: ✅ error: invalid-otp | ```dart\ntest('RF46 CP02 - OTP incorrecto: maneja error invalid-otp', () async {\n  final fakeCallable = FakeHttpsCallable((params) async {\n    throw FirebaseFunctionsException(code: 'invalid-argument', message: 'invalid-otp');\n  });\n  final service = AuthService(functions: FakeFirebaseFunctions(fakeCallable));\n  final result = await service.verificarOtpRecuperacion('test@test.com', '000000');\n  expect(result['error'], equals('invalid-otp'));\n});``` |

---

## RF-NEW — Creación de Colectivo / RF05 — Selección de Punto de Recojo

| # | Escenario | MOMENTO 1 — ¿Qué pasa si...? | MOMENTO 2 — Ficha de caso de prueba (Precondición / Acción / Resultado esperado) | MOMENTO 3 — Test en código (Dart / flutter_test) |
|---|---|---|---|---|
| CP01 | Orden paraderos | ¿Qué pasa si los paraderos se registran en desorden en la base de datos? | **Precondición**: 3 paraderos con `orden` 2, 1 y 3 en FakeDb. <br> **Acción**: `service.getParaderosActivos(ruta: 'chosica_lima')`. <br> **Resultado esperado**: Lista devuelta en orden 1, 2, 3. <br> **Resultado obtenido**: ✅ Lista ordenada | ```dart\ntest('RF-NEW CP01 - Flujo exitoso: getParaderosActivos retorna paraderos ordenados por posición en la ruta', () async {\n  final fakeFirestore = FakeFirebaseFirestore();\n  await fakeFirestore.collection('paraderos').add({ 'nombre': 'A', 'orden': 2 });\n  await fakeFirestore.collection('paraderos').add({ 'nombre': 'B', 'orden': 1 });\n  final service = ParaderoService(db: fakeFirestore);\n  final result = await service.getParaderosActivos(ruta: 'chosica_lima');\n  expect(result[0].nombre, equals('B'));\n});``` |
| CP10 | Fallback | ¿Qué pasa si la base de datos no tiene paraderos para la ruta elegida? | **Precondición**: FakeDb vacío. <br> **Acción**: `service.getParaderosActivos(...)`. <br> **Resultado esperado**: Retorna lista hardcodeada de 8 paraderos por defecto. <br> **Resultado obtenido**: ✅ 8 paraderos fallback | ```dart\ntest('RF-NEW CP10 - Ruta sin paraderos: el sistema retorna la lista de fallback cuando no hay paraderos en Firestore', () async {\n  final fakeFirestore = FakeFirebaseFirestore();\n  final service = ParaderoService(db: fakeFirestore);\n  final result = await service.getParaderosActivos(ruta: 'chosica_lima');\n  expect(result.length, equals(8));\n});``` |
| CP02 | Filtrado query | ¿Qué pasa si el pasajero busca un paradero por el nombre "chosica"? | **Precondición**: Lista de paraderos en memoria. <br> **Acción**: `service.filtrar(lista, 'chosica')`. <br> **Resultado esperado**: Retorna solo el paradero "Chosica Centro". <br> **Resultado obtenido**: ✅ Filtro aplicado | ```dart\ntest('RF05 CP02 - Filtrado por nombre: filtrar() retorna solo los paraderos que coinciden con el query', () {\n  final paraderos = [ ParaderoModel(nombre: 'Chosica Centro'), ParaderoModel(nombre: 'Ate') ];\n  final service = ParaderoService(db: FakeFirebaseFirestore());\n  final result = service.filtrar(paraderos, 'chosica');\n  expect(result.length, equals(1));\n});``` |

---

## Resumen General
- **Total de archivos de test documentados**: 13
- **Total de CPs documentados en este archivo**: 32
- **Estado de los tests**:
    - ✅ **Pasando**: 32
    - ⚠️ **Con observaciones / Deuda Técnica**: 1 (RF41)

*Documentación generada automáticamente basada en la ejecución de la suite de pruebas.*
